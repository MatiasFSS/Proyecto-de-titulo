<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Proyecto" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Proyecto" parent="Base.Theme.Proyecto">
        <item name="windowSplashScreenBackground">@color/color_navBar</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/aries</item>
        <item name="windowSplashScreenAnimationDuration">300</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.Proyecto</item>
    </style>

</resources>